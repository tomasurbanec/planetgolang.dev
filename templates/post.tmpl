<article data-id="{{ .ID }}">
<h3><a href="{{ .Url }}">{{ .Title }}</a></h3>
<div class="metadata"><strong>{{ .Author }}</strong> @ <strong>{{ .FormattedPublishedAt }}</strong> on <strong><a href="{{ .SourceUrl }}">{{ .Source }}</a></strong></div>
<p>
{{ .Summary }}
</p>
</article>
